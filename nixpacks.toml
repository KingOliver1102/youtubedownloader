# nixpacks.toml - tells Coolify how to build and run your app

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn -w 4 -b 0.0.0.0:3000 app:app"
